# Private LangGraph Configuration
# These variables are used server-side only and not exposed to the client

# Your LangGraph server URL (keep this private)
LANGGRAPH_API_URL=https://your-langgraph-server.example.com

# Your LangSmith API key (keep this private)
LANGSMITH_API_KEY=lsv2_pt_your_api_key_here

# Optional: Default assistant ID (can be overridden in settings)
NEXT_PUBLIC_ASSISTANT_ID=agent

# DO NOT set NEXT_PUBLIC_API_URL - the app now uses internal proxy only
