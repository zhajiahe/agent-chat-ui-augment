# LangGraph Agent Chat UI - Private Deployment Configuration
# Copy this file to .env.local and configure the values for your deployment

# =============================================================================
# REQUIRED: LangGraph Server Configuration (Server-side only)
# =============================================================================

# The URL of your LangGraph server (PRIVATE - not exposed to clients)
# Example: https://your-agent.default.us.langgraph.app
LANGGRAPH_API_URL=https://your-langgraph-server.example.com

# Your LangSmith API key (PRIVATE - not exposed to clients)  
# Example: lsv2_pt_abc123def456...
LANGSMITH_API_KEY=lsv2_pt_your_api_key_here

# =============================================================================
# OPTIONAL: Client-side Configuration
# =============================================================================

# Default Assistant/Graph ID (visible to clients, can be changed in settings)
# This is the ID of the assistant or graph to use by default
NEXT_PUBLIC_ASSISTANT_ID=agent

# =============================================================================
# IMPORTANT NOTES FOR PRIVATE DEPLOYMENT
# =============================================================================

# ❌ DO NOT SET: NEXT_PUBLIC_API_URL
# The application now uses an internal proxy for security.
# All API requests are automatically routed through /api/* endpoints.

# ✅ SECURITY FEATURES:
# - LangGraph server URL is completely hidden from clients
# - API credentials are stored server-side only
# - All requests go through secure internal proxy
# - Users cannot modify API server address

# =============================================================================
# DEVELOPMENT NOTES
# =============================================================================

# For local development:
# 1. Copy this file to .env.local
# 2. Configure LANGGRAPH_API_URL to point to your local/remote LangGraph server
# 3. Set LANGSMITH_API_KEY if required by your deployment
# 4. Run: pnpm dev

# For production deployment:
# 1. Set these environment variables in your deployment platform
# 2. Ensure HTTPS is enabled
# 3. Run: pnpm build && pnpm start

# =============================================================================
# TROUBLESHOOTING
# =============================================================================

# If you encounter issues:
# 1. Check the server logs for API proxy initialization messages
# 2. Verify LANGGRAPH_API_URL is accessible from your server
# 3. Confirm LANGSMITH_API_KEY has proper permissions
# 4. See PRIVATE_DEPLOYMENT.md for detailed troubleshooting guide 